import { MyfavoritePipe } from './myfavorite.pipe';

describe('MyfavoritePipe', () => {
  it('create an instance', () => {
    const pipe = new MyfavoritePipe();
    expect(pipe).toBeTruthy();
  });
});
